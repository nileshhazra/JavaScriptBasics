function greet(name) { // 'name' is the parameter
    console.log('Good Morning, ' + name);
}

greet('Bill'); // 'Bill' is the argument
greet('Sarah')

function square(number) {
    return number * number;
}

let number = square(2);
console.log(number);